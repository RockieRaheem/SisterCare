import { AppShellSkeleton } from "@/components/ui/Skeleton";

/** Consistent transition state for every route in the application. */
export default function Loading() {
  return <AppShellSkeleton />;
}
